{
  "name": "webgpu-audio-bridge",
  "version": "0.6.5",
  "description": "Schema-driven lock-free SPSC SharedArrayBuffer ring for streaming structured frames from a Web Worker (typically WebGPU compute) into an AudioWorklet — the control-rate / audio-rate bridge pattern.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE",
    "CITATION.cff",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsx tests/schema.test.ts && tsx tests/Bridge.test.ts && tsx tests/Bridge.phaseLock.test.ts && tsx tests/Bridge.concurrent.test.ts && tsx tests/Float64RingBuffer.test.ts && tsx tests/Float64RingBuffer.concurrent.test.ts",
    "test:unit": "tsx tests/schema.test.ts && tsx tests/Bridge.test.ts && tsx tests/Bridge.phaseLock.test.ts && tsx tests/Float64RingBuffer.test.ts",
    "test:concurrent": "tsx tests/Bridge.concurrent.test.ts && tsx tests/Float64RingBuffer.concurrent.test.ts",
    "test:browser": "playwright test --config tests/browser/playwright.config.ts",
    "bench": "tsx bench/Bridge.bench.ts && tsx bench/Float64RingBuffer.bench.ts",
    "bench:e2e": "node bench/e2e-latency/serve.mjs",
    "dev:demo": "node examples/minimal/serve.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run test && npm run build"
  },
  "keywords": [
    "webgpu",
    "audioworklet",
    "audio",
    "ring-buffer",
    "sharedarraybuffer",
    "spsc",
    "lock-free",
    "atomics",
    "control-rate",
    "streaming",
    "web-audio"
  ],
  "author": "Creeptone",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Creeptones/webgpu-audio-bridge.git"
  },
  "homepage": "https://github.com/Creeptones/webgpu-audio-bridge#readme",
  "bugs": {
    "url": "https://github.com/Creeptones/webgpu-audio-bridge/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.19.41",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
