# Repo guidance for Claude (and humans)

This file documents project-specific conventions that apply to any session working in this repo. Read it before starting work.

## Versioning policy — slow down on minor bumps

Pre-1.0 the project has been moving fast: `0.1.x → 0.2.0 → 0.3.0 → 0.4.0 → 0.4.1 → 0.5.0 → 0.6.0` in a short window, one minor bump per shipped improvement. **Going forward, slow down.** The 1.0 release should be a substantive milestone — many additional improvements are planned before it lands, and we don't want the public version number to inflate past the maturity it reflects.

**Default policy from 0.6.0 onward:**

- **Patch bumps (`0.6.0 → 0.6.1 → 0.6.2 …`) are the default** for any wire-compatible improvement. Smoothers, new helper APIs, perf wins, new docs, additional test pins, bug fixes, internal refactors — all patch-level.
- **Minor bumps (`0.6.x → 0.7.0`) are reserved for**:
  - Wire-format changes (new active lanes, frame-size additions, breaking SAB layout shifts).
  - Public-API breaking changes (renamed/removed methods, changed return types).
  - Accumulated patches reaching a coherent "release moment" worth calling out — let several patches land first, then promote in a batch.
- **Major bump (`0.x → 1.0.0`) is the deliberate stability commitment.** Do not bump there for a single feature; bump there when the API is settled and the project is ready to make compat promises.

**Practical rule for any session that ships an improvement:**

1. Default to a patch bump.
2. Only consider a minor bump if the change breaks wire format or breaks the public TS surface.
3. If unsure, ask the user before bumping `0.x.0`. A `0.6.1` that the user later promotes to `0.7.0` is cheap; a premature `0.7.0` they have to walk back is not.

The rapid 0.4 → 0.6 sequence was justified by each improvement adding a wire-format change AND a substantial public-API addition simultaneously. That bundling shouldn't be the norm — future improvements should usually land as `0.6.x` patches and accumulate.

**Extended slowdown from 0.7.0 onward (post-0.6.9 policy update).** Each minor-level cohort should reach deep into the patch space before promoting:

- `0.7.0 → 0.7.1 → 0.7.2 → … → 0.7.99` is the **expected** patch lifetime for a minor cohort. Don't promote to `0.8.0` after a handful of patches — let the patches accumulate and use each one as a checkpoint to assess "are we ready for 1.0 yet?"
- The same applies at every subsequent minor: `0.8.x` should likewise go deep before `0.9.0`, and `0.9.x` before `1.0.0`.
- Rationale: 80% of the polish toward 1.0 happens in the last 20% of the work. Treating minor bumps as cheap promotions inflates the version number past the actual maturity. Treating patches as effectively unbounded gives us a clean "assess at every step" cadence without ever feeling pressure to ship a premature 1.0.

The minor-bump triggers remain unchanged from the 0.6.x rule (wire-format change, breaking public-API change, or a deliberate "coherent release moment" promotion) — this rule just raises the bar on the third trigger. A coherent release moment that would have landed as `0.7.5 → 0.8.0` should now land as `0.7.5 → 0.7.6` and the promotion question revisited after another N patches.

If a session is genuinely unsure whether the change warrants a minor bump under this stricter rule, default to the patch bump and let the user promote later — same asymmetry as the original 0.6.x rule.

**Three-digit patch in the late `0.9.x` run (post-0.9.900 policy update).** Starting at **`0.9.900`** the patch field is **three digits**: `0.9.900 → 0.9.901 → … → 0.9.999`. This is a deliberate decision to reserve ~100 ordered checkpoints between here and `1.0.0`, so the long final-polish run toward 1.0 has room to land one patch per shipped improvement without ever feeling pressure to promote early. (`0.9.900` is the renumbered successor to the briefly-tagged `0.9.90`; semver ordering is preserved because `900 > 89 > 9`.) The next patch after `0.9.900` is **`0.9.901`**, not `0.9.91`. The minor-bump triggers are unchanged — `1.0.0` is still the deliberate stability commitment, reached when the API is settled, not when the patch counter happens to approach `999`. If `0.9.999` is reached before 1.0 is ready, widen again (`0.9.9990`) rather than forcing a premature `1.0.0`.

## Commit policy

- Each release-grade change gets its own commit with a multi-line message (subject line names the version + tagline; body describes what shipped, why, and any wire-compat notes).
- Mirror the prior CHANGELOG style — a session that bumps to e.g. `0.6.1` should append a `[0.6.1]` block above the previous entry with the same section shape (`### Added` / `### Why` / `### Wire compatibility` / `### Tests` / `### Documentation`).
- Include `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` (or current model identifier) in the commit trailer when Claude wrote the code.
- Never push to `origin/main` without the user's explicit OK. Local commits are fine; remote pushes require permission.

## Test / bench gates before any version bump

Mandatory before any commit that bumps the version:

```bash
npm run typecheck   # tsc --noEmit, must be clean
npm test            # all 6 suites green (schema / Bridge / Bridge.phaseLock / Bridge.concurrent / Float64RingBuffer / Float64RingBuffer.concurrent)
npm run bench       # push/pull/pullLatest median sanity-check vs documented baseline (~1.20 μs at N=1000)
```

The concurrent test has a known timing-sensitive `emptyWaitTimeouts === 0` assertion that can flake once on a loaded machine; re-run once if it fires, treat as real only if reproducible. Document any new flake patterns in the relevant test file's header.

## What lives where

- `src/Bridge.ts` — the primary public surface. ~1,134 lines as of 0.6.9. Header comment blocks document every lane and every protocol invariant; keep them current when changing behavior. Bridge composes one `SpscRing<S>` (`this.ring`), one `FrameSmoother<S>` (`this.smoother`), and one `ConsumerClockRecovery` (`this.pll`) — every public method is either a delegator or the invariant-classifier orchestration.
- `src/SpscRing.ts` — internal SAB / Atomics core (0.6.8 extract). ~866 LOC. Composes one `AdaptiveFlowController` for the lane-2 PI tick. Internal-only through 0.6.9; 0.6.10 is the class-promotion patch. Public method surface grew at 0.9.31 with `drainNoNotify(out, maxCount?)`, the promoted form of the previously `BridgeInputLane.pullAll`-internal amortized-notify drain primitive — `BridgeInputLane.pullAll` is now a one-line forwarder.
- `src/MpmcRing.ts` — 0.9.907 (Apollo Frontier 3, Stage 1) wait-free MP→SC fan-in ring. The first production code of Frontier 3: its OWN SAB layout (header + per-slot generation region + payload), entirely separate from `SpscRing` (the frozen SPSC core is never touched). Implements **Policy B (envelope-guaranteed)** — producer fetch-add ticket + per-slot generation release, with a producer-side drop-newest envelope check (SLACK = `producerCount − 1`); consumer is an O(1) head check with strict `d == 0` gate + W-skip overload net. Hard wait-free both sides. **Internal-first + `@experimental`** (mirrors SpscRing internal@0.6.8 → public@0.6.10): NOT exported from `src/index.ts`; a one-shot construction warning fires. Self-contained file header documents the layout + invariants + memory-ordering argument. Proven by `tests/MpmcRing.interleaving.test.ts` (exhaustive fuzzer w/ INV-W wait-free witness), `tests/MpmcRing.test.ts` (API pins), `tests/MpmcRing.concurrent.test.ts` (1.2 M-frame `worker_threads` bit-exact stress, via `tests/_mpmcStress.ts` + `tests/_mpmcStress.worker.ts`). Registered in `test` / `test:unit` (interleaving + API) / `test:concurrent`.
- `src/connectFanIn.ts` — 0.9.909 (Apollo Frontier 3, Stage 3) the `connect()`-style MP→SC fan-in topology constructor over `MpmcRing`. `connectFanIn(spec) → FanInTopology` allocates + `initLayout`s the shared SAB once (fixed `producerCount` → handle); `mountFanIn(handle, {role})` reconstructs an `MpmcRing` on each peer via the **bare ctor** (no re-init); both roles return the raw ring. **Turbo-ONLY** (non-isolated → `ConnectUnsupportedError('isolation-required')`, NO MessageChannel fallback). Sizing (`FanInSizing`) reuses `connect()`'s heuristic then guards `capacity = nextPow2(max(backlog+SLACK, producerCount+1))` (surfaces `reservedSlack`/`usableDepth`). **Self-contained — never opens `connect.ts`** (duplicates a tiny layout-match), so the SPSC bit-exact gate is structural. Exported from `src/experimental/index.ts` (subpath), NOT root, until `MpmcRing` promotes. Proven by `tests/connectFanIn.test.ts` (9 single-thread pins) + `tests/connectFanIn.concurrent.test.ts` (1.2 M-frame stress through the wiring, reuses `tests/_mpmcStress.ts`). Browser smoke `examples/mpmc-fan-in/` (`npm run dev:mpmc-fan-in`, port 5184). Registered in `test`/`test:unit` (single-thread) + `test`/`test:concurrent` (cross-thread).
- `src/SpmcRing.ts` — 0.9.911 (Apollo Frontier 3, Stage 4.1) wait-free SP→MC **broadcast** fan-out ring (one producer → N consumers, every consumer sees every frame via its OWN cursor). Its OWN SAB layout (header + per-consumer `dequeuePos`/`dropped`/`tornGuarded` lane region + per-slot generation region + payload), separate from the frozen `SpscRing` AND `MpmcRing`. Implements **Policy P1 (two-phase seqlock)**: producer `push` stores `Busy(T)=2T+1` BEFORE the payload, `Complete(T)=2T` after, plain-advances `writeTicket`, laps freely, NEVER reads consumer cursors (fully decoupled — a stuck consumer never back-pressures the source); consumer `pull(out, consumerIndex)` is the O(1) seqlock double-check (gate `d=signedDiff(seq1, 2D)` → read → re-read `seq2`; deliver iff `seq2===seq1` else counted `tornGuarded` drop) + W-skip overload net. **Generation lane in DOUBLED units (`2·ticket`, +1 busy); cursors in ticket units — don't mix.** `consumerCount` fixed at allocation (max 64, sizes the per-consumer region); each consumer mounts with a `consumerIndex`. Hard wait-free both sides. **Internal-first + `@experimental`** (mirrors MpmcRing internal@0.9.907): exported from `src/experimental/index.ts`, NOT `src/index.ts`; one-shot construction warning. Proven by `tests/SpmcRing.interleaving.test.ts` (exhaustive fuzzer porting `bench/spmc-probe.mjs`'s DFS — INV-1 no-torn / INV-2 conservation / INV-3 per-consumer FIFO+broadcast / INV-W wait-free witness, + the `twoPhase=false ⇒ torn` and `recheck=false ⇒ torn` negative pins), `tests/SpmcRing.test.ts` (10 API pins), `tests/SpmcRing.concurrent.test.ts` (role-flipped vs MpmcRing — REAL producer on main + N inline-eval consumer workers, 3×1 M frames bit-exact, `tornGuarded=0`; reuses `tests/_mpmcStress.ts`). Registered in `test`/`test:unit` (interleaving + API) + `test`/`test:concurrent` (stress). The `connectFanOut()` constructor over it shipped at 0.9.928 (next bullet).
- `src/connectFanOut.ts` — 0.9.928 (Apollo Frontier 3, Stage 4.3) the `connect()`-style SP→MC **broadcast** topology constructor over `SpmcRing` — the direct sibling of `connectFanIn`. `connectFanOut(spec) → FanOutTopology` allocates + `initLayout`s the shared SAB once (fixed `consumerCount` → handle); `mountFanOut(handle, {role, consumerIndex?})` reconstructs a `SpmcRing` on each peer via the **bare ctor** (no re-init). **Three differences from `connectFanIn`:** (1) `consumerCount` fixed at allocation (sizes the per-consumer lane region, max 64); (2) each consumer mounts a distinct `consumerIndex ∈ [0, consumerCount)`, the producer mounts unbound + MUST NOT pass one (`mountFanOut` enforces — genuinely new vs fan-in's advisory role); (3) **NO reserved slack** — the Policy-P1 producer laps freely + never reads consumer cursors, so capacity IS the lap window (`usableDepth === capacity`, `reservedSlack === 0`; a consumer lagging > capacity drops oldest, counted). **Turbo-ONLY** (non-isolated → `ConnectUnsupportedError('isolation-required')`, no MessageChannel fallback). **Self-contained — never opens `connect.ts`** (duplicates a tiny layout-match), so the SPSC bit-exact gate stays structural; no new formal model (pure wiring over the proven Stage-4.0 protocol). Exported from `src/experimental/index.ts`, NOT root, until `SpmcRing` promotes. Proven by `tests/connectFanOut.test.ts` (10 single-thread pins incl. the broadcast bit-exact round-trip + the consumerIndex contract) + `tests/connectFanOut.concurrent.test.ts` (3×1 M-frame broadcast stress THROUGH the wiring, `dropped`/`tornGuarded` = 0, asserts the topology sized the SAB to the `SpmcRing` layout; reuses `tests/_mpmcStress.ts`). Registered in `test`/`test:unit` (single-thread) + `test`/`test:concurrent` (cross-thread). No browser demo yet (deferred).
- `src/jit/` — **The Autonomous JIT** (Apollo Frontier 5). A developer's naive scalar JS DSP kernel → auto-vectorized WASM SIMD, proven bit-exact by an equivalence gate, live-swapped into a running AudioWorklet click-free. **`@experimental`**, exported from `src/experimental/index.ts` (subpath) — NOT `src/index.ts`; the subtree transitively imports `acorn` (the JS parser, ONLY in `parse.ts`), which the zero-runtime-dep core never reaches (pinned by the import-graph guard in `tests/JitCompiler.test.ts`). One-shot construction `console.warn`. Pipeline: `compileKernel(source, signature, { compileWat })` (Stage 1a @ 0.9.913) parses (acorn) → lowers/validates → vectorizes → emits scalar+SIMD WAT → runs the equivalence **gate** (`gate.ts` — SIMD ≡ scalar reference bit-exact/within-ULP over a fuzz corpus + the user's JS as a third oracle) → discriminated union (`accepted`/`rejected-source`/`rejected-gate`/`unsupported`; rejection is a VALUE, never throws). `CompileWat` (WAT→bytes) is INJECTED — the core ships no encoder (tests/bench inject wabt). `JitKernelConsumer` (Stage 1b @ 0.9.914, exact-lerp @ 0.9.915) is the worklet-side runtime: holds the permanent JS fallback + the compiled SIMD `Instance`, instantiates SYNCHRONOUSLY between quanta (`installCompiledKernel`/`…FromBytes`, ~4 µs — `port.onmessage` ONLY, never `process()`), runs both kernels into disjoint scratch slabs during the fade and EXACT-LERP amplitude-crossfades, degrading to JS on EVERY failure (`JitKernelSwap` is the pure swap schedule). `connectJit.ts` (Stage 3 @ 0.9.917) is the one-call constructor + 3-realm wiring: `connectJit` (main) / `runJitCompile` (compile-worker) / `createJitConsumer`+`handleJitInstallMessage` (worklet) / `forwardCompileResponse` — the single transport-choice point, **bytes default, Module opt-in** (a `WebAssembly.Module` silently fails to deserialize into an AudioWorklet realm — an async `messageerror`, not a send throw — so bytes is the robust worklet transport). Proven by `tests/JitCompiler.*` (compiler/gate + negative pins), `tests/JitKernelSwap.test.ts` + `tests/JitKernelConsumer.test.ts` (runtime), `tests/connectJit.test.ts` (wiring), `tests/browser/jit-vectorize.spec.ts` (cross-engine smoke). Bench `bench/jit.bench.ts` (`npm run bench:jit` — 3.8×–9.2× SIMD, ~4 µs install, ~0 glitch, fade quantum ~0.04 % of budget). Demo `examples/jit-vectorize/` (`npm run dev:jit-vectorize`, port 5185 — wabt + acorn vendored; `serve.mjs` rewrites the bare `acorn` import since import maps don't work in workers). Design note `docs/jit-vectorize-design.md`. **Promotion to the 1.0 core stays deferred** (let it soak; the `@experimental` warning stays). **Apollo Frontier 6 (the language→music layer) lives in the same subtree.** `kernelGrammar.ts` (Stage 0 @ 0.9.918) is the closed postfix token serialization of the IR — the lossless codec (`kernelToTokens`/`tokensToKernel`), the value-returning SYNTAX validator (`validateTokens`, gate #1 of 3: syntax → equivalence → acoustic), the copy-pasteable flat text form (`tokensToString`/`parseTokens`), and the deterministic content hash (`kernelHash` = FNV-1a-64 over `kernelKey`, a cache-key/identity NOT a security boundary; regression pin `kernelHash(gain) === "72b5c2e5a7a5f117"`). Streams are SELF-CONTAINED (the `param` tokens carry the signature) and the token path is **acorn-free** (pure data over the IR types). Stage 1 (@ 0.9.919) is the model-free compile pipeline + cache: `compileIr(ir, opts)` is the IR back-half extracted from `compileKernel` (vectorize → emit → gate; `compileKernel` = `parse → lower → compileIr`, byte-for-byte behavior-preserving — pin 8 determinism + pin 10 import-graph intact); `compileTokens(tokens, opts)` = `validateTokens` → `compileIr`, surfacing a syntax failure as a `rejected-source` **`E_TOKENS`** diagnostic (the new closed-enum member; the grammar's `{ error, at? }` bridged to `Diagnostic` with the token index in the message); `emitJsKernel(ir)` (`emitJsKernel.ts`) inverts `lower.ts` to the worklet JS fallback — tree-shape + number faithful, reserved-word-safe name aliasing (both kernels are called POSITIONALLY so aliasing is invisible), `neg(const c) ↔ const(-c)` the one IR↔JS gap (folded in the round-trip pin); `KernelCache`/`CharacterizedKernel` (`kernelCache.ts`) content-addresses by `kernelHash` (hit returns the same object with no recompile; `acoustic` now POPULATED by gate #3 — Stage 2). `connectJit({ tokens, signature })` adds the token path alongside `{ kernel }` (`JitCompileRequest` is now a `kind`-discriminated union, JS arm's `kind` optional → backward-compatible). Proven by `tests/kernelGrammar.test.ts` (Stage 0 pins A–G) + `tests/compileTokens.test.ts` (Stage 1: palette accept/bit-exact, `E_TOKENS` reject, cache hit-identity + compile-count probe, `emitJsKernel` round-trip) + `tests/connectJit.test.ts` pin G (token path e2e bit-exact). Design note `docs/frontier6-grammar-design.md`. Browser demo `examples/kernel-palette/` (`npm run dev:kernel-palette`, port 5186 — 0.9.920) makes the token path + cache tangible: pick a TOKEN kernel from a palette → a background compile+cache worker holding ONE `KernelCache` `getOrCompile`s it (syntax + equivalence gate, or an instant hit), `connectJit({ tokens })` builds the worklet plumbing, and the gate-verified SIMD bytes live-swap in click-free; re-picking a kernel is a visible cache HIT. Sibling of `examples/jit-vectorize/` (the JS-path off-thread-compile demo); reuses the COOP/COEP + bare-`acorn`-rewrite `serve.mjs` and the vendored wabt/acorn. Stage 2 (@ 0.9.921) is the **acoustic gate (gate #3)**: `acousticGate.ts` — `acousticGate(ir, opts) → { ok, profile } | { ok:false, profile, reason }` (rejection a VALUE) runs the equivalence-accepted IR over a fixed DETERMINISTIC probe (a bin-aligned full-scale sine, distinct harmonic per input) and returns an `AcousticProfile` (`finite` across ALL outputs + `rms`/`peak`/`dcOffset`/`crestFactor`/`spectralCentroid` + a 64-band-default (`fingerprintBands`) **L1-normalized** `magnitude` fingerprint, amplitude-invariant ⇒ "sounds-like" vector; default bumped 16→64 at 0.9.926 so the embedding discriminates low harmonics — gate verdict unchanged, only the vector is finer), ACCEPTing iff finite + within sane bounds (`maxPeak`/`maxAbsDcOffset`/`maxCrestFactor`, generous defaults 1e3/1e3/1e4). **No wasm** — gate #2 already proved SIMD ≡ the scalar IR reference, so it profiles `evalReference(ir, inputs, scalars, n)` (an exported, reusable pure JS IR interpreter rounding every leaf+op to width via `Math.fround` for f32 ⇒ bit-identical to the scalar WASM). Deterministic (no `Date.now`/`Math.random`; stock radix-2 FFT). **The cache layer OWNS gate #3**: `getOrCompile` runs it after the equivalence accept, attaches the profile to the `CharacterizedKernel` (`acoustic` now REQUIRED, computed once per content hash, free on a hit) or returns the new **`rejected-acoustic`** verdict (in `GetOrCompileResult` ONLY — `compileIr`/`compileTokens` untouched) and does NOT store; `GetOrCompileOptions.acoustic` tunes it. Proven by `tests/acousticGate.test.ts` (palette sane + gain fingerprint pinned; multiply-overflow `(x·3e38)·3e38` → `non-finite` & enormous-gain `×1e9` → `peak-out-of-bounds`, both PASS gate #2 yet rejected by gate #3 & not cached — a div-by-0 can't make the point since `f32x4.div(0,0)` ≠ scalar NaN payload; determinism + `evalReference` math; cache attaches + hit returns gate-free). The palette demo surfaces the fingerprint (worker forwards `acoustic`, HUD sparkline). Stage 3a (@ 0.9.922) is **`legalNextTokens` — the constrained-decoder mask**: `validateTokens` is refactored into a single reusable grammar step machine (`GrammarState`/`stepGrammar`/`finalizeGrammar`, behavior-preserving — Stage-0 pins A–G + the `kernelHash(gain)` pin intact, `validateTokens` now a fold), and `legalNextTokens(prefix) → { kinds, done }` is its forward reading over the SAME machine (the legal next-token KIND set off the final state + `done === validateTokens(prefix).ok`). Sharing one machine is the load-bearing property — the decoder's logit mask CANNOT drift from the validator (it is, by construction, exactly the kinds the validator won't reject). The mask is the operand-stack arity function (value-pushers always; `unary`@d≥1; `binary`@d≥2; `store`@d==1; `{width}`→`{param,bound}` in the header); **Stage-3a masks KINDS** (a wrong OPERAND was still rejectable — that's Stage 3a+). The model-free safety contract (a mask-respecting emitter can only produce `validateTokens`-accepted streams) is proven by `tests/legalNextTokens.test.ts` (5 pins: exact-mask completeness over the corpus, depth rules, the 1000-seed no-invalid-stream emitter, 1000 random valid kernels, invalid-prefix→empty + refactor parity). Stage 3a+ (@ 0.9.924) is **`legalNextOperands` — the operand mask**: `legalNextOperands(prefix, kind) → OperandChoices` masks the operand VALUES of a chosen kind off the SAME `GrammarState` (non-drift extends to operands) — enumerable fields as arrays (`width`→2 widths; `param` role→4 roles; `unary`/`binary`→the six ops; `load`/`store`/`scalar`/`bound` names→declared params), unbounded fields as validity predicates (`const`→finite; affine stride/intercept→integer; bound-const→non-neg integer; param name→fresh-IDENT). **Role-partitioned** (a deliberate SOUND tightening of `validateTokens`: load⊂inputs, store⊂outputs, scalar⊂scalars, bound-param⊂lengths — every role-correct name is a declared name, so still validates) ⇒ a genuine refinement over the kind mask (no input declared ⇒ kind mask still admits `load` but the operand `arrays` set is empty). Returns `{}` for an invalid prefix OR an illegal kind, so the two masks compose. Proven by `tests/legalNextOperands.test.ts` (5 pins: operand soundness over the corpus, per-kind sets exact incl. the empty-set/illegal/invalid refinements, the 1000-seed **no-invalid-TOKEN** emitter filling BODY operands ONLY from the mask, 1000 random valid kernels, parity + barrel identity). Composed, the two masks make the Stage-3b SLM a pure, fully-guarded emitter swap with zero ways to emit a rejected token. The Stage-3b SLM plugs in behind these masks and swaps ONLY the emitter. **Quick-win #1 (@ 0.9.925) is the negative cache**: `KernelCache.getOrCompile` now memoizes REJECTIONS, not just accepts — two memos because the gates fail at two addressabilities (a SYNTAX reject `rejected-source` has no IR → keyed by the flat `tokensToString` stream text, checked BEFORE `validateTokens`; a BODY reject `unsupported`/`rejected-gate`/`rejected-acoustic` has a valid IR → keyed by `kernelHash`, the SAME content address as the positive store, so a different-signature/same-body stream hits it). The two maps can't collide on a key and a deterministic gate gives one verdict per address ⇒ the negative cache can't shadow a later accept. EVERY `GetOrCompileResult` now carries a `cached` flag (true ⇒ served from a memo, no gate re-run); new surface `RejectVerdict` + `KernelCache.rejectedSize`, `clear()` wipes both stores. Kills a generative emitter's reroll waste + is the read the Stage-3b reward loop will key off. Proven by `tests/kernelCache.negativeCache.test.ts` (4 pins: syntax memoized, body memoized w/ a compile-count probe showing an acoustic-reject HIT invokes `compileWat` ZERO times, unsupported memoized, coexists-with-positive + `clear` wipes both). **Quick-win #2 (@ 0.9.926) is `fingerprint.ts`** — "sounds-like" queries over `AcousticProfile.magnitude` (the amplitude-invariant embedding): `fingerprintDistance` (L2, a proper metric), `nearestByFingerprint`, `dedupByFingerprint` (greedy ε-collapse), `sortByBrightness`/`brighterThan`/`darkerThan` (the `spectralCentroid` axis); pure (no wasm), `FingerprintLike`/`FingerprintMatch` types, exported from the experimental subpath. **SOUNDNESS (load-bearing):** distance 0 = "sounds identical on the ONE probe", NOT a behavioral-equivalence proof (a clipper and a linear gain match on a non-clipping probe) — use it to CULL/SEARCH, never to SUBSTITUTE a kernel in a signal path; `kernelHash` stays the sound identity. Paired with the default `fingerprintBands` 16→64 bump (so the embedding discriminates harmonics by default). Proven by `tests/fingerprint.test.ts` (4 pins: metric laws, amplitude-invariance + dedup, timbre-NN cubic4→cubic, brightness ordering). **Quick-win #3 (@ 0.9.927) is `corpusIndex.ts`** — the offline "sounds-like" index (build/worker-time tooling, pure, no wasm): `characterizeCorpus(items, toKernel)` (acousticGate each → `{ entries, rejected }`, a toKernel-throw OR a gate rejection captured as a `CorpusRejection`, never thrown), `clusterByFingerprint(entries, radius)` (leader clustering — admit by distance-to-seed, then re-pick each cluster's `prototype` as its MEDOID + report the realized radius; `radius 0` = exact dedup-by-sound), `buildCorpusIndex(items, toKernel, {radius, acoustic})` (one-call), `corpusPrototypes(index)` (the medoid seed set); generic over the item type `T`, types `CorpusEntry`/`CorpusRejection`/`CorpusCluster`/`CorpusIndex`/`BuildCorpusIndexOptions`, experimental-subpath exports. Inherits the fingerprint soundness caveat (a prototype is a representative SOUND, not a canonical impl; `kernelHash` stays identity). Proven by `tests/corpusIndex.test.ts` (4 pins: accept/reject split, radius-0 identical-sound grouping, radius-0.08 cubic-variant fold + medoid radius, build+prototypes determinism). **All three model-free quick-wins (#1 negative cache, #2 fingerprint, #3 corpus index) are now shipped.** Still `@experimental` (the grammar/codec/hash/pipeline/acoustic gate/decoder masks are outside the 1.0 contract until they soak + promote).
- `src/FrameSmoother.ts` / `src/ConsumerClockRecovery.ts` / `src/AdaptiveFlowController.ts` — the three internal heap-state machines extracted in 0.6.9. ~312 / ~134 / ~131 LOC. Each carries a self-contained file header documenting invariants + the math. Internal-only through 0.6.9.
- `src/schema.ts` — DSL + compile pass. `.withInvariant(fn, { absoluteEpsilon? })` (0.6.6 added the epsilon opts).
- `src/trajectory.ts` — order-1/2/3 Taylor evaluator with optional safety clamps (0.6.7). Fast / clamped paths split per-spec.
- `src/Float64RingBuffer.ts` — deprecated legacy class. Frozen at v0.1.x byte format. Removal scheduled no earlier than 2.0.
- `tests/Bridge.test.ts` — 63 single-thread pins as of 0.6.9 (file header lists each by number). New pins append at the end with a numbered header comment and get added to `main()`'s call list. Use the existing `assert` / `assertEq` / `ok` helpers from `tests/_assert.ts`; no test framework.
- `tests/Bridge.concurrent.test.ts` — 1 M-frame cross-thread SPSC stress. Producer is an inline-eval Worker; uses `Bridge.describeLayout()` so schema changes auto-propagate.
- `tests/Bridge.phaseLock.test.ts` — FFT-based phase-lock spectrum pin (added 0.6.4).
- `bench/Bridge.bench.ts` — push / pull / pullLatest cells + 0.5.0's `flow_scale recovery` characterization cell + 0.6.7's `trajEval (fast)` / `trajEval (clamp)` cells.
- `bench/mpmc.bench.ts` — 0.9.908 (Apollo Frontier 3, Stage 2) `MpmcRing` characterization bench (`npm run bench:mpmc`). Four cells: push/pull latency vs `producerCount` (proves producerCount-invariance), MP→SC-vs-SPSC side-by-side (poll-only MP→SC can undercut the notify-bearing SPSC pull), drop-rate at the envelope edge (measured == analytic `(r−1)/r`, torn/overrun = 0), and a `worker_threads` contention curve (throughput + drop% vs N, zero-tear asserted). Gates push/pull < 10 µs at every P. The SPSC bench is structurally unchanged (the primitive never touches `SpscRing`).
- `CHANGELOG.md` — newest entry at top. Entries follow the established structure.
- `README.md` — public docs; mirror CHANGELOG entries for shipped features under the relevant section (API reference, Back-pressure, Roadmap, etc.).
- `docs/standard-mode-design.md` — 0.9.39 design note covering the `MessageChannelBridge<S>` (Standard mode) shape, versioning, and scope decisions; followed by a "Shipped postscript" documenting the actual 0.9.40 ship.
- `docs/hybrid-residual-comparison.md` — 0.9.42 design note covering the hybrid residual-on-carrier pattern (0.9.41) vs six alternative GPU-audio approaches + a 15-item gap roadmap.
- `formal/MpmcRing.tla` / `.cfg` — 0.9.906 (Apollo Frontier 3, Stage 0) TLA+/PlusCal model of the additive MP→SC `MpmcRing` protocol (multiple fetch-add producers, per-slot generation release, single in-order consumer); the frozen `formal/SpscRing.tla` is untouched. Documented in `formal/README.md` "MP→SC model".
- `docs/mpmc-happens-before-proof.md` — 0.9.906 written happens-before proof for the MP→SC ring + the Stage-0 finding (Policy A unsound → Policy B envelope-guaranteed, enforced producer-side).
- `bench/mpmc-probe.mjs` — 0.9.906 throwaway, dependency-free (`node bench/mpmc-probe.mjs`) exhaustive interleaving probe; the runnable half of Stage 0. **Superseded at Stage 1 (0.9.907) by `tests/MpmcRing.interleaving.test.ts`**, which ports the same DFS state machine into the in-CI `assert` harness and adds the INV-W wait-free witness. The probe may be deleted; kept for now as a zero-dep reference.
- `formal/SpmcRing.tla` / `.cfg` — 0.9.910 (Apollo Frontier 3, Stage 4.0) TLA+/PlusCal model of the additive **SP→MC broadcast** `SpmcRing` protocol (one in-order lap-freely producer, many independent consumers each with their own cursor + a **two-phase seqlock** double-check — `Busy(T)` gen store before payload, `Complete(T)` after; the consumer gates on `gen==Complete(head)`, reads, then re-reads and discards if changed). Models the **sound Policy P1** (decoupled producer, per-consumer seqlock guard). Invariants `NoTornRead`/`PerConsumerFifo`/`Conservation` + the `assert ~cDirty`/`assert cRead=D` Commit-step witnesses + liveness `EventuallyDrained`/`HeadProgress`. The frozen `formal/SpscRing.tla` and `formal/MpmcRing.tla` are untouched. Documented in `formal/README.md` "SP→MC model".
- `docs/spmc-happens-before-proof.md` — 0.9.910 written happens-before proof for the SP→MC broadcast ring (the seqlock release/acquire edge, per-consumer no-torn/FIFO/conservation/broadcast-consistency, wait-freedom) + the **Stage-4.0 finding**: the handoff's producer sketch (a single gen store **after** the payload, no busy marker) is **unsound** — a lap-behind consumer delivers torn bytes because the gen doesn't move until after the overwrite, so the re-read sees no change; the sound design is the **two-phase seqlock** (busy marker before payload). Settles **P1 (lap-freely + guard, recommended)** vs **P2 (envelope-against-slowest, optional lossless mode)**.
- `bench/spmc-probe.mjs` — 0.9.910 throwaway, dependency-free (`node bench/spmc-probe.mjs`) exhaustive interleaving probe; the runnable half of Stage 4.0 and sibling of `bench/mpmc-probe.mjs` (producer/consumer roles flipped, the seqlock guard the central step). Scenario A proves P1 two-phase seqlock sound (0 torn / 0 stall / O(1)); Scenario B exhibits the single-store sketch tearing on a lap-behind consumer; Scenario C shows dropping the re-read tears even with two-phase. **To be superseded at Stage 4.1 by `tests/SpmcRing.interleaving.test.ts`** (ports the DFS into the in-CI `assert` harness + INV-W witness + the negative pins). The probe may be deleted once that lands.
- `LLM_BUNDLE.md` — generated single-file digest of README + ROADMAP + recent CHANGELOG + design notes + canonical source files. `.gitignore`d as a build artifact; regenerate via `npm run llm-bundle` (`scripts/regenerate-llm-bundle.mjs`). Useful for handing context to a new LLM session in one read.

## Where the bridge is consumed

The website twin at `../NewProject/website/src/lib/wavefunction/gpu/Float64RingBuffer.ts` is a separately-maintained copy that's gradually being migrated to consume this package directly. See `../NewProject/website/WebsitePlans/WebAudioBridge Improvements 4 - Migration to Bridge in website twin.md` for the migration plan. Changes to this repo's public API should be conscious of the website consumer, but the website is not a release blocker — it pulls via `npm link` and can update at its own cadence.
